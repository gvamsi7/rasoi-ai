'use client';
import {useEffect,useState} from 'react';import {getFavorites,toggleFavorite} from '@/lib/storage';import {Heart} from 'lucide-react';
export default function Favorites(){const [items,setItems]=useState<{slug:string;title:string}[]>([]);useEffect(()=>setItems(getFavorites()),[]);return <main><nav className="nav"><a className="logo" href="/">🥘 Rasoi <span>AI</span></a><a className="outline" href="/">Home</a></nav><section className="content narrow"><p className="eyebrow">YOUR SAVED RECIPES</p><h1>Favorites</h1>{items.length?<div className="shopping">{items.map(x=><div className="shopping" key={x.slug}><a href={`/recipes/${x.slug}`}><strong>{x.title}</strong></a><button onClick={()=>setItems(toggleFavorite(x))}><Heart size={17} fill="currentColor"/></button></div>)}</div>:<div className="empty">No favorites yet. Save a recipe from the home page.</div>}</section></main>}
