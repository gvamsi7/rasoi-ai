import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';
const user=async(req:Request)=>{const email=req.headers.get('x-user-email');if(!email)return null;return prisma.user.upsert({where:{email},update:{},create:{email}})};
export async function GET(req:Request){const u=await user(req);if(!u)return NextResponse.json({error:'Authentication required'},{status:401});return NextResponse.json(await prisma.favorite.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'}}))}
export async function POST(req:Request){const u=await user(req);if(!u)return NextResponse.json({error:'Authentication required'},{status:401});const {recipeSlug}=await req.json();if(!recipeSlug)return NextResponse.json({error:'recipeSlug required'},{status:400});return NextResponse.json(await prisma.favorite.upsert({where:{userId_recipeSlug:{userId:u.id,recipeSlug}},update:{},create:{userId:u.id,recipeSlug}}))}
export async function DELETE(req:Request){const u=await user(req);if(!u)return NextResponse.json({error:'Authentication required'},{status:401});const {recipeSlug}=await req.json();await prisma.favorite.deleteMany({where:{userId:u.id,recipeSlug}});return NextResponse.json({ok:true})}
