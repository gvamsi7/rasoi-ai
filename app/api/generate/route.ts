import {NextResponse} from 'next/server';
export async function POST(req:Request){const {ingredients,meal='dinner',preferences={}}=await req.json();if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:'Add OPENAI_API_KEY to .env.local',demo:true});const prompt=`Create one practical Indian ${meal} recipe using ${ingredients}. Preferences: ${JSON.stringify(preferences)}. Return JSON with title,description,ingredients,steps,nutrition (protein,fiber,fats,calories).`;const r=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:'gpt-4o-mini',messages:[{role:'user',content:prompt}],response_format:{type:'json_object'}})});if(!r.ok)return NextResponse.json({error:'AI generation failed'},{status:500});const j=await r.json();return NextResponse.json(JSON.parse(j.choices[0].message.content));}