import {NextResponse} from 'next/server';
export async function POST(req:Request){try{const {ingredients,meal='dinner',preferences={}}=await req.json();if(!Array.isArray(ingredients)||!ingredients.length)return NextResponse.json({error:'Add at least one ingredient.'},{status:400});if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:'OPENAI_API_KEY is not configured.'},{status:503});const prompt=`You are Rasoi AI, an Indian home-cooking assistant. Create a practical ${meal} recipe using these available ingredients: ${ingredients.join(', ')}. Preferences: ${JSON.stringify(preferences)}. Prioritize familiar Indian cooking, affordable ingredients, realistic quantities, protein, fiber and healthy fats. Return ONLY valid JSON with title, description, ingredients (array of strings with quantities), steps (array of strings), prepTime (number of minutes), servings (number), and nutrition object with protein, fiber, fats, calories (all numbers).`;const r=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-4o-mini',messages:[{role:'system',content:'Return concise valid JSON only.'},{role:'user',content:prompt}],response_format:{type:'json_object'},temperature:.7})});if(!r.ok)return NextResponse.json({error:'OpenAI request failed.'},{status:502});const j=await r.json();const content=j.choices?.[0]?.message?.content;if(!content)throw new Error('Empty AI response');return NextResponse.json(JSON.parse(content))}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Generation failed'},{status:500})}}
